# ==========================================
# PHP FreeBase - Environment Configuration
# ==========================================
# Copy this file to .env and adjust the values for your environment.
# Never commit your real .env file to version control.

# Application Environment ('development' or 'production')
APP_ENV=development
APP_NAME="PHP FreeBase"
APP_URL=http://localhost:8000

# Database Connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=freebase
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Session & Security Settings
SESSION_NAME=freebase_sec_session
SESSION_LIFETIME=3600            # Idle timeout in seconds (1 hour)
SESSION_MAX_LIFETIME=28800       # Absolute timeout in seconds (8 hours)

# Brute Force Protection (Database-backed)
LOGIN_MAX_ATTEMPTS=5             # Max failed attempts before lockout
LOGIN_LOCKOUT_SECONDS=300        # Lockout duration in seconds (5 minutes)

# Password Reset & Email Verification Token Lifetimes
RESET_TOKEN_LIFETIME=3600        # Reset token validity in seconds (1 hour)
VERIFY_TOKEN_LIFETIME=86400      # Email verification token validity (24 hours)

# Emergency Admin Recovery Secret (Optional)
# If configured, enables /emergency-reset.php to reset the admin password.
# Leave empty to keep emergency reset endpoint completely disabled.
ADMIN_RECOVERY_SECRET=

# Timezone
APP_TIMEZONE=Europe/Madrid
